<UserControl x:Class="DbManager.Wpf.Views.ConnListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#DCDCDC" BorderThickness="0,0,0,1" Padding="4,2">
            <StackPanel Orientation="Horizontal">
                <Button ToolTip="新建连接" Command="{Binding AddConnectionCommand}" Padding="6,3" Margin="1,0"
                        Style="{StaticResource ToolBarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="新建" Margin="4,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button ToolTip="编辑连接" Command="{Binding EditConnectionCommand}" Padding="6,3" Margin="1,0"
                        Style="{StaticResource ToolBarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PencilOutline" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="编辑" Margin="4,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button ToolTip="删除连接" Command="{Binding DeleteConnectionCommand}" Padding="6,3" Margin="1,0"
                        Style="{StaticResource ToolBarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="删除" Margin="4,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Separator Margin="4,2"/>
                <Button ToolTip="测试连接" Command="{Binding TestConnectionCommand}" Padding="6,3" Margin="1,0"
                        Style="{StaticResource ToolBarButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="LanConnect" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="测试" Margin="4,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Separator Margin="4,2"/>
                <Button ToolTip="刷新" Command="{Binding RefreshCommand}" Padding="6,3" Margin="1,0"
                        Style="{StaticResource ToolBarButton}">
                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- 连接列表 -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Connections}"
                  SelectedItem="{Binding SelectedConnection}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" FontSize="12"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F8F8"
                  BorderThickness="0"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="130"/>
                <DataGridTextColumn Header="类型" Binding="{Binding DbType}" Width="80"/>
                <DataGridTextColumn Header="主机" Binding="{Binding Host}" Width="130"/>
                <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="60"/>
                <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="90"/>
                <DataGridTextColumn Header="数据库" Binding="{Binding DbName}" Width="100"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
